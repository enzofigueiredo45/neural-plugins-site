{
  "name": "neural-plugins-site",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check main.js && node --check lib/db.js && node --check lib/validation.js && node --test",
    "security": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "connect-redis": "^10.0.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "express-session": "^1.17.3",
    "helmet": "^8.3.0",
    "pg": "^8.11.0",
    "qrcode": "^1.5.1",
    "redis": "^6.2.0",
    "speakeasy": "^2.0.0",
    "sqlite3": "^6.0.1",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
