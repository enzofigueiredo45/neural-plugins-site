{
  "name": "neural-plugins-site",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check main.js && node --check lib/db.js && node --check lib/mercado-pago.js && node --check lib/validation.js && node --test",
    "security": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "connect-redis": "^7.0.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "express-session": "^1.17.3",
    "helmet": "^6.0.1",
    "pg": "^8.11.0",
    "qrcode": "^1.5.1",
    "redis": "^4.6.7",
    "speakeasy": "^2.0.0",
    "sqlite3": "^6.0.1",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
